#ifndef LOGIN
	#define LOGIN
	int loginValidation(char username[], char password[]);	//checks if username and password matches with the any of the ones in the login file
	void login();	//gets user input and makes sure password on screen is hidden
#endif
